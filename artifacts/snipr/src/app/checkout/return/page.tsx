import { Suspense } from "react";
import CheckoutReturn from "@/views/CheckoutReturn";

export default function CheckoutReturnPage() {
  return (
    <Suspense>
      <CheckoutReturn />
    </Suspense>
  );
}
